"use client";

/**
 * Dashboard catalog view — re-exports the Scale Systems AI catalog
 * so /dashboard?view=catalog stays aligned with /catalog.
 */
export { default } from "@/components/catalog/ScaleSystemsCatalog";
